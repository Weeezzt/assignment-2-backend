<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" media="screen" href="/main.css"/>
    <script type="module" src="https://md-block.verou.me/md-block.js"></script>
    <title>movie</title>
</head>
<body>
    <%- include('./partials/header.ejs') %>
    <h1 class="movie__title">Heres a little more about <%=movie.title%></h1>

    <div class="movie">
        <%if (movie.title) {%>
            <h2 class="title">
            <%=movie.title%>
        </h2>
        <%}%>
        
        <%if (movie.image) {%>
            <img src="<%=movie.image.url%>" alt="Movie poster not found" class="poster">
        <%}%>

        <%if (movie.intro) {%>
           <md-block>
            <%=movie.intro%>
           </md-block>
        <%}%>
    </div>
    <%- include('./partials/footer.ejs') %>
</body>
</html>